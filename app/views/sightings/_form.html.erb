<%= form_with(model: sighting) do |form| %>
  <% if sighting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sighting.errors.count, "error") %> prohibited this sighting from being saved:</h2>

      <ul>
        <% sighting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.label :time, style: "display: block" %>
    <%= form.time_field :time %>
  </div>

  <div>
    <%= form.label :ufo_id, style: "display: block" %>
    <%= form.text_field :ufo_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
